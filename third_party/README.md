# Third-Party Repositories

Add policy and benchmark repositories here as pinned git submodules.

Default rule: submodule upstream directly when it works unchanged. Fork under `ripl` only when an evaluation needs patches, a frozen branch, or paper-critical reproducibility.

