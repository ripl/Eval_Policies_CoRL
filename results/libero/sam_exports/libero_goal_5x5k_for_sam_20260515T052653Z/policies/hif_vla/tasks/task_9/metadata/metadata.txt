run_group=hif_vla_goal_5k_full_2042379_20260514T131958Z
task_id=9
date_utc=2026-05-14T13:20:04+00:00
hostname=g6.ttic.edu
cuda_visible_devices=0
policy=hif_vla
suite=libero_goal
checkpoint=/share/data/ripl/tianchong/projects/Eval_Policies_CoRL/checkpoints/hifvla-libero-goal
init_root=/share/data/ripl/tianchong/projects/Eval_Policies_CoRL/artifacts/libero/init_state_goal_5000
hif_overlay=/share/data/ripl/tianchong/projects/Eval_Policies_CoRL/envs/hif_vla_overlay
episodes_per_task=500
save_videos=False
433a1530b0739b066110157fe5244d6afccf70d5
M  .gitmodules
 M scripts/simplerenv/run_cogact_bridge.sh
 M scripts/slurm/simplerenv_cogact_bridge.sbatch
A  third_party/gr1
A  third_party/roboflamingo
Am third_party/simplerenv_openvla
A  third_party/x_vla
A  third_party/xvla_simpler_env
?? patches/
?? scripts/calvin/
?? scripts/libero/
?? scripts/simplerenv/run_spatialvla_bridge.sh
?? scripts/simplerenv/run_xvla_widowx.sh
?? scripts/simplerenv/setup_spatialvla_env.sh
?? scripts/simplerenv/setup_xvla_env.sh
?? scripts/slurm/calvin_gr1_smoke.sbatch
?? scripts/slurm/calvin_roboflamingo_smoke.sbatch
?? scripts/slurm/calvin_xvla_smoke.sbatch
?? scripts/slurm/cosmos_goal_smoke.sbatch
?? scripts/slurm/cosmos_model_download.sbatch
?? scripts/slurm/hif_vla_goal_5k_array.sbatch
?? scripts/slurm/hif_vla_goal_download.sbatch
?? scripts/slurm/hif_vla_goal_smoke.sbatch
?? scripts/slurm/libero_openvla_oft_goal_5k_array.sbatch
?? scripts/slurm/libero_openvla_oft_goal_smoke.sbatch
?? scripts/slurm/libero_pi05_goal_5k_array.sbatch
?? scripts/slurm/libero_pi05_goal_smoke.sbatch
?? scripts/slurm/libero_spatial_forcing_goal_5k_array.sbatch
?? scripts/slurm/setup_simplerenv_spatialvla.sbatch
?? scripts/slurm/setup_simplerenv_xvla.sbatch
?? scripts/slurm/simplerenv_spatialvla_bridge.sbatch
?? scripts/slurm/simplerenv_xvla_widowx.sbatch
?? scripts/slurm/simvla_goal_5k_array.sbatch
?? scripts/slurm/simvla_goal_smoke.sbatch
?? scripts/slurm/simvla_stage.sbatch
?? third_party/alphabrain/
?? third_party/hif_vla/
?? third_party/lerobot/
?? third_party/simvla/
a76afc997b1924de619f6d3b288adeeb6c55dade
diff --git a/experiments/robot/libero/run_libero_eval.py b/experiments/robot/libero/run_libero_eval.py
index 21f0e75..a9c4e69 100644
--- a/experiments/robot/libero/run_libero_eval.py
+++ b/experiments/robot/libero/run_libero_eval.py
@@ -8,6 +8,7 @@ import json
 import logging
 import os
 import sys
+import csv
 from collections import deque
 from dataclasses import dataclass
 from enum import Enum
@@ -111,7 +112,7 @@ class GenerateConfig:
     task_suite_name: str = TaskSuite.LIBERO_10  # Task suite
     num_steps_wait: int = 10                         # Number of steps to wait for objects to stabilize in sim
     num_trials_per_task: int = 50                    # Number of rollouts per task
-    initial_states_path: str = "DEFAULT"             # "DEFAULT", or path to initial states JSON file
+    initial_states_path: str = "DEFAULT"             # "DEFAULT", path to init-state root, or legacy JSON file
     env_img_res: int = 256                           # Resolution for environment images (not policy input resolution)
 
     #################################################################################################################
@@ -119,6 +120,13 @@ class GenerateConfig:
     #################################################################################################################
     run_id_note: Optional[str] = None                # Extra note to add to end of run ID for logging
     local_log_dir: str = "./experiments/logs"        # Local directory for eval logs
+    max_total_episodes: Optional[int] = None         # Optional hard cap across all tasks for smoke tests
+    episode_results_path: Optional[str] = None       # Optional per-episode CSV output path
+    policy_name: str = "hif_vla"                     # Name to write in per-episode CSV rows
+    selected_task_id: int = -1                       # If >= 0, evaluate only this task index
+    episode_start_idx: int = 0                       # Inclusive episode index within each task
+    episode_end_idx: int = -1                        # Exclusive episode index; -1 means num_trials_per_task
+    save_videos: bool = False                        # Whether to save rollout videos for each episode
 
     use_wandb: bool = False                          # Whether to also log results in Weights & Biases
     wandb_entity: str = "your-wandb-entity"          # Name of WandB entity
@@ -228,19 +236,25 @@ def log_message(message: str, log_file=None):
 
 def load_initial_states(cfg: GenerateConfig, task_suite, task_id: int, log_file=None):
     """Load initial states for the given task."""
-    # Get default initial states
-    initial_states = task_suite.get_task_init_states(task_id)
-
-    # If using custom initial states, load them from file
-    if cfg.initial_states_path != "DEFAULT":
-        with open(cfg.initial_states_path, "r") as f:
-            all_initial_states = json.load(f)
-        log_message(f"Using initial states from {cfg.initial_states_path}", log_file)
-        return initial_states, all_initial_states
-    else:
+    if cfg.initial_states_path == "DEFAULT":
+        initial_states = task_suite.get_task_init_states(task_id)
         log_message("Using default initial states", log_file)
         return initial_states, None
 
+    custom_path = Path(cfg.initial_states_path)
+    if custom_path.is_dir():
+        task = task_suite.get_task(task_id)
+        init_states_file = custom_path / task.problem_folder / task.init_states_file
+        initial_states = torch.load(init_states_file)
+        log_message(f"Using initial states from {init_states_file}", log_file)
+        return initial_states, None
+
+    initial_states = task_suite.get_task_init_states(task_id)
+    with open(custom_path, "r") as f:
+        all_initial_states = json.load(f)
+    log_message(f"Using initial states from {cfg.initial_states_path}", log_file)
+    return initial_states, all_initial_states
+
 
 def prepare_observation(obs, resize_size):
     """Prepare observation for policy input."""
@@ -264,6 +278,36 @@ def prepare_observation(obs, resize_size):
     return observation, img  # Return both processed observation and original image for replay
 
 
+def append_episode_result(cfg: GenerateConfig, row: dict) -> None:
+    """Append one per-episode result row for paired-policy analysis."""
+    if cfg.episode_results_path is None:
+        return
+
+    output_path = Path(cfg.episode_results_path)
+    output_path.parent.mkdir(parents=True, exist_ok=True)
+    fieldnames = [
+        "suite",
+        "task_id",
+        "task_name",
+        "task_description",
+        "episode_idx",
+        "instance_id",
+        "total_episode_idx",
+        "policy_name",
+        "checkpoint_ref",
+        "initial_states_path",
+        "success",
+        "num_env_steps",
+        "error_type",
+    ]
+    write_header = not output_path.exists()
+    with output_path.open("a", newline="") as f:
+        writer = csv.DictWriter(f, fieldnames=fieldnames)
+        if write_header:
+            writer.writeheader()
+        writer.writerow(row)
+
+
 def process_action(action, model_family):
     """Process action before sending to environment."""
     # Normalize gripper action [0,1] -> [-1,+1] because the environment expects the latter
@@ -331,6 +375,8 @@ def run_episode(
 
     # Setup
     t = 0
+    num_env_steps = 0
+    error_type = ""
     replay_images = []
     max_steps = TASK_MAX_STEPS[cfg.task_suite_name]
 
@@ -346,12 +392,14 @@ def run_episode(
             # Do nothing for the first few timesteps to let objects stabilize
             if t < cfg.num_steps_wait:
                 obs, reward, done, info = env.step(get_libero_dummy_action(cfg.model_family))
+                num_env_steps += 1
                 t += 1
                 continue
 
             # Prepare observation
             observation, img = prepare_observation(obs, resize_size)
-            replay_images.append(img)
+            if cfg.save_videos:
+                replay_images.append(img)
 
             frame_buf.append(img)
 
@@ -384,6 +432,7 @@ def run_episode(
 
             # Execute action in environment
             obs, reward, done, info = env.step(action.tolist())
+            num_env_steps += 1
             if done:
                 success = True
                 break
@@ -391,9 +440,10 @@ def run_episode(
             
 
     except Exception as e:
+        error_type = type(e).__name__
         log_message(f"Episode error: {e}", log_file)
 
-    return success, replay_images
+    return success, replay_images, num_env_steps, error_type
 
 
 def run_task(
@@ -423,12 +473,16 @@ def run_task(
 
     # Start episodes
     task_episodes, task_successes = 0, 0
-    for episode_idx in tqdm.tqdm(range(cfg.num_trials_per_task)):
+    episode_end_idx = cfg.num_trials_per_task if cfg.episode_end_idx < 0 else cfg.episode_end_idx
+    for episode_idx in tqdm.tqdm(range(cfg.episode_start_idx, episode_end_idx)):
+        if cfg.max_total_episodes is not None and total_episodes >= cfg.max_total_episodes:
+            break
+
         log_message(f"\nTask: {task_description}", log_file)
 
         # Handle initial state
-        if cfg.initial_states_path == "DEFAULT":
-            # Use default initial state
+        if all_initial_states is None:
+            # Use default or artifact-root initial state
             initial_state = initial_states[episode_idx]
         else:
             # Get keys for fetching initial episode state from JSON
@@ -446,7 +500,7 @@ def run_task(
         log_message(f"Starting episode {task_episodes + 1}...", log_file)
 
         # Run episode
-        success, replay_images = run_episode(
+        success, replay_images, num_env_steps, error_type = run_episode(
             cfg,
             env,
             task_description,
@@ -468,10 +522,29 @@ def run_task(
             task_successes += 1
             total_successes += 1
 
-        # Save replay video
-        # save_rollout_video(
-        #     replay_images, total_episodes, success=success, task_description=task_description, log_file=log_file
-        # )
+        append_episode_result(
+            cfg,
+            {
+                "suite": cfg.task_suite_name,
+                "task_id": task_id,
+                "task_name": task.name,
+                "task_description": task_description,
+                "episode_idx": episode_idx,
+                "instance_id": f"{cfg.task_suite_name}/{task.name}/{episode_idx:04d}",
+                "total_episode_idx": total_episodes - 1,
+                "policy_name": cfg.policy_name,
+                "checkpoint_ref": str(cfg.pretrained_checkpoint),
+                "initial_states_path": cfg.initial_states_path,
+                "success": int(success),
+                "num_env_steps": num_env_steps,
+                "error_type": error_type,
+            },
+        )
+
+        if cfg.save_videos:
+            save_rollout_video(
+                replay_images, total_episodes, success=success, task_description=task_description, log_file=log_file
+            )
 
         # Log results
         log_message(f"Success: {success}", log_file)
@@ -526,7 +599,11 @@ def eval_libero(cfg: GenerateConfig) -> float:
 
     # Start evaluation
     total_episodes, total_successes = 0, 0
-    for task_id in tqdm.tqdm(range(num_tasks)):
+    task_ids = [cfg.selected_task_id] if cfg.selected_task_id >= 0 else range(num_tasks)
+    for task_id in tqdm.tqdm(task_ids):
+        if cfg.max_total_episodes is not None and total_episodes >= cfg.max_total_episodes:
+            break
+
         total_episodes, total_successes = run_task(
             cfg,
             task_suite,
Python 3.10.16
GPU 0: NVIDIA RTX A6000 (UUID: GPU-c22a3f04-6447-c044-4086-a9293147cfbf)
NVIDIA RTX A6000, GPU-c22a3f04-6447-c044-4086-a9293147cfbf, 580.95.05, 49140 MiB
